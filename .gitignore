node_modules/
.DS_Store
*.tx
.github_ssh_key
.deploy_key
.claude/
public/editor.bundle.js
public/dialer.bundle.js

# Build specs may contain live credentials — never commit
fable5*.md

# Bank statement exports — never commit transaction history
*.csv
*.CSV
