{
  "name": "workspace",
  "version": "1.0.0",
  "description": "Unified notes + kanban workspace",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "esbuild src/editor.js --bundle --minify --outfile=public/editor.bundle.js",
    "build:dialer": "esbuild src/dialer.js --bundle --minify --outfile=public/dialer.bundle.js"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "express": "^4.18.3",
    "multer": "^1.4.5-lts.1",
    "twilio": "^6.0.2",
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "@codemirror/commands": "^6.3.3",
    "@twilio/voice-sdk": "^2.18.3",
    "@codemirror/search": "^6.5.6",
    "@codemirror/state": "^6.4.0",
    "@codemirror/view": "^6.26.0",
    "esbuild": "^0.20.2"
  }
}
